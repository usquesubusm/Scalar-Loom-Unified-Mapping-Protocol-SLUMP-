// HoneyEngine.hpp
// The Scalar Loom Unified Mapping Protocol V1.0 August 14, 2025
// Honey License v1.2 — Symbolic Commons Operating Agreement (compat. CC BY-NC-SA 4.0)
#pragma once
#include <atomic>

namespace sproot {

class HoneyEngine {
public:
  void reseedPulse() { pulses_++; }
  long pulses() const { return pulses_.load(); }
private:
  std::atomic<long> pulses_{0};
};

} // namespace sproot
